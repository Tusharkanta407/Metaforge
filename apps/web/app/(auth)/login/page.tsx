export default function LoginPage() {
  return (
    <main>
      <h1>Login</h1>
      <p>Continue with Google (Firebase).</p>
      <button type="button">Login with Google</button>
    </main>
  );
}
